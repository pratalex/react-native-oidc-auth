{
  "name": "react-native-oidc-auth-root",
  "version": "0.1.3",
  "private": true,
  "description": "OIDC authentication for React Native with shared core and ready-to-use implementations for Expo and Bare React Native.",
  "workspaces": {
    "packages": [
      "packages/**"
    ]
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "publish-packages": "lerna publish",
    "version-packages": "lerna version"
  },
  "author": "pratalex",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "lerna": "^9.0.0"
  },
  "packageManager": "yarn@3.8.7"
}
